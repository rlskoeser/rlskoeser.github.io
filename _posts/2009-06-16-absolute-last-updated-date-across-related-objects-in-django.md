---
layout: post
title: Absolute Last Updated date across related objects in Django
created: 1245173356
categories: django snippets
---
<p>I have short chain of One To Many related objects and needed a function I could call at any level in the chain that would return the date of the most rescently updated item anywhere in the chain.</p><p>There's no direct method in Django for this but I had an implementation that we might find useful to reuse so I'm pasing a code snippet here.&nbsp; Essentially I just put an 'all_last_updated' method in each model that gives me the value I'm looking for light so:</p><p>class Project(models.Model):<br />&nbsp;&nbsp;&nbsp; ...<br />&nbsp;&nbsp;&nbsp; last_updated = models.DateField(auto_now=True, auto_now_add=True)<br /><br />&nbsp;&nbsp;&nbsp; def all_last_updated(self):<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; try:<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; lastDate = [self.last_updated, self.release_set.latest().all_last_updated()]<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; lastDate.sort()<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; lastDate.reverse()<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; return lastDate[0]<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; except:<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; return self.last_updated<br /><br />&nbsp;&nbsp;&nbsp; class Meta:<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; get_latest_by = 'last_updated'<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; ordering = ['last_updated']</p><p>class Release(models.Model):<br />&nbsp;&nbsp;&nbsp; ...<br />&nbsp;&nbsp;&nbsp; last_updated = models.DateField(auto_now=True, auto_now_add=True)<br /><br />&nbsp;&nbsp;&nbsp; def all_last_updated(self):<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; try:<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; lastDate = [self.last_updated, self.activity_set.latest().last_updated]<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; lastDate.sort()<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; lastDate.reverse()<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; return lastDate[0]<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; except:<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; return self.last_updated<br /><br />&nbsp;&nbsp;&nbsp; class Meta:<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; get_latest_by = 'last_updated'<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; ordering = ["project_fk", "internal_priority"]<br /><br />class Activity(models.Model):<br />&nbsp;&nbsp;&nbsp; ...<br />&nbsp;&nbsp;&nbsp; last_updated = models.DateField(auto_now=True, auto_now_add=True)<br /><br />&nbsp;&nbsp;&nbsp; def all_last_updated(self):<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; '''<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Date of last update from this or any child object.<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; '''<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; return self.last_updated<br /><br />&nbsp;&nbsp;&nbsp; class Meta:<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; get_latest_by = 'last_updated'<br />&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; ordering = ['created_on']</p><p>This method relies on having 'get_latest_by' defined in each models Meta subclass so I can use the .latest() method, which returns the most rescent item in a QuerySet.&nbsp; </p><p>This allows me to call the related sets .latest() method and compare it to that objects last_updated and return the more rescent date.</p><p>I had to execute this all within a try block as a QuerySet will return a DoesNotExist error when calling .latest() in this way if there were no records in the related set.</p><p>Other than that it seems to work just fine and I hope it's useful.</p>
